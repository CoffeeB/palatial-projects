{
  "compilerOptions": {
    "baseUrl": ".",  // Make sure baseUrl is set to the project root
    "paths": {
      "@/*": ["src/*"],  // Map @/ to the src directory
      "@/lib/*": ["src/lib/*"] // Explicit mapping for lib
    }
  }
}
